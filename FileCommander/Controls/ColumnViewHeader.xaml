<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FileCommander.Controls.ColumnViewHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileCommander.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding ColumnWidths[0]}" />
            <ColumnDefinition Width="{Binding ColumnWidths[1]}" />
            <ColumnDefinition Width="{Binding ColumnWidths[2]}" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Name" HorizontalAlignment="Stretch" Margin="3, 0, 0, 0"/>
            <local:BorderGrid Grid.Column="1" Width="5px" Background="Transparent" BorderBrush="DarkGray" BorderThickness="0, 0, 1, 0" />
        </Grid>
        <Grid Grid.Column="1" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Datum" HorizontalAlignment="Stretch" Margin="3, 0, 0, 0" />
            <local:BorderGrid Grid.Column="1" Width="5px" Background="Transparent" BorderBrush="DarkGray" BorderThickness="0, 0, 1, 0" />
        </Grid>
        <Grid Grid.Column="2" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Größe" HorizontalAlignment="Stretch" Margin="3, 0, 0, 0"/>
            <local:BorderGrid Grid.Column="1" Width="5px" Background="Transparent" BorderBrush="DarkGray" BorderThickness="0, 0, 1, 0" />
        </Grid>
    </Grid>
</UserControl>
