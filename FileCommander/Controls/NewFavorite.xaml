<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FileCommander.Controls.NewFavorite"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileCommander.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Name="root">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Margin="0, 7, 8, 0" Text="Name:"></TextBlock>
        <TextBlock Margin="0, 7, 8, 0" Text="Pfad:" Grid.Row="1"></TextBlock>
        <TextBox Text="{Binding ElementName=root, Path=FavoriteName, Mode=TwoWay}" Grid.Column="1" GotFocus="TextBox_GotFocus" />
        <TextBox Text="{Binding ElementName=root, Path=Path, Mode=TwoWay}" Grid.Column="1" Grid.Row="1" GotFocus="TextBox_GotFocus" />
    </Grid>
</UserControl>
