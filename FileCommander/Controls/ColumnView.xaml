<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FileCommander.Controls.ColumnView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:FileCommander.Controls"
    xmlns:template="using:FileCommander.DataTemplates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DataTemplates/ParentItemDataTemplate.xaml" />
                <ResourceDictionary Source="/DataTemplates/DirectoryItemDataTemplate.xaml" />
                <ResourceDictionary Source="/DataTemplates/FileItemDataTemplate.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <template:TemplateSelector x:Key="TemplateSelector">
                <template:TemplateSelector.Templates>
                    <template:NamedTemplate ItemName="ParentItem" ItemDataTemplate="{StaticResource ParentItemDataTemplate}"/>
                    <template:NamedTemplate ItemName="DirectoryItem" ItemDataTemplate="{StaticResource DirectoryItemDataTemplate}"/>
                    <template:NamedTemplate ItemName="FileItem" ItemDataTemplate="{StaticResource FileItemDataTemplate}"/>
                </template:TemplateSelector.Templates>
            </template:TemplateSelector>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ColumnViewHeader Grid.Row="0" x:Name="Header" />

        <ScrollViewer Grid.Row="1"
                  VerticalScrollBarVisibility="Visible"
                  HorizontalScrollBarVisibility="Disabled">
            <ItemsRepeater Grid.Row="1" x:Name="ListView" 
               ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                           ScrollViewer.VerticalScrollBarVisibility="Visible"
               IsTabStop="False"
               KeyDown="ListView_KeyDown"
               ItemTemplate="{StaticResource TemplateSelector}"
               GotFocus="ListView_GotFocus">
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</UserControl>
