<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FileCommander.Controls.FolderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileCommander.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:FileCommander.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox x:Name="PathTextBox" KeyDown="TextBox_KeyDown" TabIndex="2" 
          Text="{Binding CurrentPath}"
          GotFocus="TextBox_GotFocus" BorderThickness="0" FontSize="12"  />
        <controls:ColumnView Grid.Row="1" x:Name="ColumnView" x:FieldModifier="private" TabIndex="1"/>
    </Grid>
</UserControl>
