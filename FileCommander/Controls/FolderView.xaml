<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FileCommander.Controls.FolderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileCommander.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:FileCommander.Controls"
    mc:Ignorable="d"
    Loaded="UserControl_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox x:Name="PathTextBox" KeyDown="TextBox_KeyDown" GotFocus="PathTextBox_GotFocus" MinHeight="0" Height="20"
                 Text="{Binding CurrentPath}" Margin="0" Padding="0" BorderThickness="0" FontSize="12" />
        <controls:VirtualTable Grid.Row="1" x:Name="VirtualTable" TabIndex="1"/>
    </Grid>
</UserControl>
