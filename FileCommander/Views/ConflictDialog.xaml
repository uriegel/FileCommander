<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FileCommander.Views.ConflictDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileCommander.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" x:Name="root" Title="Konflikte" Closed="root_Closed">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="24" KeyDown="Grid_KeyDown">
        <Grid.Resources>
            <DataTemplate x:Key="ItemDataTemplate">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <Grid Margin="2, 0, 5, 0"  >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </Grid>
            </DataTemplate>
        </Grid.Resources>        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="12" Margin="0,24">
            <ProgressBar Height="4" IsIndeterminate="True" FlowDirection="{Binding ElementName=root, Path=FromRight, Converter={ StaticResource FromRightConverter}}"/>
            <TextBlock Name="Description" FontSize="24" FontWeight="SemiBold" TextWrapping="Wrap"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Name="Scroller" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
            <ItemsRepeater x:Name="ListView" ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                   ScrollViewer.VerticalScrollBarVisibility="Visible" IsTabStop="False"
                   ItemTemplate="{StaticResource ItemDataTemplate}">
            </ItemsRepeater>
        </ScrollViewer>

        <Grid Grid.Row="2" HorizontalAlignment="Right" Width="300">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.Children>
                <Button Name ="Yes" Content="Ja" HorizontalAlignment="Stretch" Margin="5" />
                <Button Name="No" Grid.Column="1" Content="Nein" HorizontalAlignment="Stretch" Margin="5" />
            </Grid.Children>
        </Grid>
    </Grid>
</Window>
